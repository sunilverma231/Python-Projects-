friends = ["jim", "kaven", "karien"]
#for index in range(len(friends)):
    #print(friends[index])
    # still having confusion in this for loop need to see more vedios
for index in range(10):
    if index == 0:
        print("first iteration")
    else:
        print("Not first")
